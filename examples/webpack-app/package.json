{
  "name": "webpack-app",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "node ./build.mjs",
    "build:serve": "pnpm run build && pnpx serve dist",
    "build:external": "PWA_CONFIG=external node ./build.mjs",
    "build:override": "PWA_CONFIG=override node ./build.mjs"
  },
  "dependencies": {
    "@vite-pwa/workbox-swkit": "workspace:*"
  },
  "devDependencies": {
    "@vite-pwa/workbox-build": "workspace:*",
    "webpack": "catalog:webpack5"
  }
}
